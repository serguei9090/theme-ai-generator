{
  "name": "@theme-ai/mcp-server",
  "version": "0.1.0",
  "private": true,
  "main": "src/server.ts",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "zod": "^4.3.6",
    "@theme-ai/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  },
  "bin": {
    "theme-ai-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/server.ts --outfile dist/index.js --bundle --target bun",
    "compile": "bun build src/server.ts --compile --outfile mcp-server"
  }
}
